//import { createStore } from "redux";
//import reducer from "dkasdlsal"
//let store = createStore(reducer); // Creates the store from the State received from the reducer(s)
//let store=createStore();
//export default store;
